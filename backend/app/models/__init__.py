# PulseDebug AI — backend/app/models/__init__.py
# Purpose:
#   This file makes the models/ directory a Python package.
#   It is intentionally empty — no models are defined here
#   because PulseDebug AI uses raw SQLite queries directly
#   rather than an ORM like SQLAlchemy.
#   This keeps the codebase simple and dependency-free.