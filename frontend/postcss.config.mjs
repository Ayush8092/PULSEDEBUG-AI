/**
 * PulseDebug AI — PostCSS Configuration
 * File: frontend/postcss.config.mjs
 * Purpose: Enables Tailwind CSS and autoprefixer in the Next.js build.
 */
const config = {
  plugins: { tailwindcss: {}, autoprefixer: {} },
};
export default config;